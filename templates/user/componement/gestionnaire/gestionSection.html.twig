  <div class="col-md-3">
  <h3> Gestion des Sections</h3>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Nom de la filiere</th>
                <th scope="col">Année</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            {% for section in sections %}

                <form class="formEventTab">
                    <div class="form-group">
                        <tr id="tableau">
                            <td scope="row"><input type="text" name="filiere" value="{{ section.filiere }}" disabled></td>  
                            <td scope="row"><input type="text" name="annee" value="{{ section.annee }}" disabled></td>  
                            <td> <button class="edit_section button_icon" type="button"> <i class="fa-solid fa-pen"></i> </button> <button class="button_icon" name="validIcon" type="submit" hidden> <i class="fa-solid fa-check"></i> </button> </td>
                            <td class ="icone"> <button class="button_icon" type="submit"> <i class="fa-solid fa-trash-can"></i> </button></td>
                            <input type="hidden"  value="/section/edit/{{ section.id }}" name="linkEdit">
                            <input type="hidden"  value="/section/delete/{{ section.id }}" name="linkDelete">
                        </tr>
                    </div>
                </form>
            {% endfor %}
        </tbody>
    </table>
</div>